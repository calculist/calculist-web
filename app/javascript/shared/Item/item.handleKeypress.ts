import cursorPosition from '../../client/ui/cursorPosition';
import commandTypeahead from '../../client/ui/commandTypeahead';

const item_handleKeypress = (function (cursorPosition, commandTypeahead) {

  return function(e) {
    // commandTypeahead.update(e);
  };

})(cursorPosition, commandTypeahead);

export default item_handleKeypress;
